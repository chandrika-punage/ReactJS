import React from "react";

class Movie extends React.Component{
    render(){
        return <diV>
            <h2>Movie Compoents</h2>
        </diV>
    }
}
export default Movie;

//we can't write return in functions so we use react.components
//overiding render method to 

//file name nd component name should be same